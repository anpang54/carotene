

// Prints the first 10 numbers of the Fibonacci sequence using an unoptimized, recursive function.


func fibonacci(n) {
    if(n <= 1) {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

repeat(10): $i{
    print(fibonacci(i), false);
    print(" ", false);
}

print("");